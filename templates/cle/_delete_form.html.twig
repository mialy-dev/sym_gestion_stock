<form method="post" action="{{ path('app_cle_delete', {'id': t_cle.id}) }}" onsubmit="return confirm('vous êtes sur de le supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_cle.id) }}">
    <button class="btn btn-danger w-50">Supprimer</button>
</form>
