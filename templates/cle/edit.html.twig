{% extends 'base.html.twig' %}

{% block content %}
    <h4>Modifier le clé</h4>

    {{ include('cle/_form.html.twig', {'button_label': 'Modifier'}) }}

   <br> <a href="{{ path('app_cle_index') }}" class="btn btn-dark w-50">Retour aux listes</a>

    {{ include('cle/_delete_form.html.twig') }}
{% endblock %}
