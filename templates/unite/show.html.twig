{% extends 'base.html.twig' %}


{% block content %}
    <h1>Unite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tunite.id }}</td>
            </tr>
            <tr>
                <th>Unite</th>
                <td>{{ tunite.unite }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-info" href="{{ path('app_unite_index') }}">Retour aux listes</a>

    <a class="btn btn-outline-success" href="{{ path('app_unite_edit', {'id': tunite.id}) }}">Modifier</a>

    {{ include('unite/_delete_form.html.twig') }}
{% endblock %}
