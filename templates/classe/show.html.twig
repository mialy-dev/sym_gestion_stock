{% extends 'base.html.twig' %}

{% block content %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tclasse.id }}</td>
            </tr>
            <tr>
                <th>Classe</th>
                <td>{{ tclasse.classe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classe_index') }}" class="btn btn-info">Retour à la liste</a>

    <a href="{{ path('app_classe_edit', {'id': tclasse.id}) }}"  class="btn btn-success">Modifier</a>

  {{ include('classe/_delete_form.html.twig') }} 
{% endblock %}
