<form method="post" action="{{ path('app_classe_delete', {'id': tclasse.id}) }}" onsubmit="return confirm('Vous voulez vraiment effacer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tclasse.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
