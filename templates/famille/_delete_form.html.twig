<form method="post" action="{{ path('app_famille_delete', {'id': tfamille.id}) }}" onsubmit="return confirm('Vos voulez vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tfamille.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
