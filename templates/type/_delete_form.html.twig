<form method="post" action="{{ path('app_type_delete', {'id': ttype.id}) }}" onsubmit="return confirm('Vous êtes sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ttype.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
