<form method="post" action="{{ path('app_departement_delete', {'id': tdepartement.id}) }}" onsubmit="return confirm('Vous êtes sûr de l'effacer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tdepartement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
